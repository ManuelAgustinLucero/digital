{% extends 'base.html.twig' %}

{% block body %}
    <h1>Comentarios list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Comentario</th>
                <th>Fechacreacion</th>
                <th>Fechaactualizacion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for comentario in comentarios %}
            <tr>
                <td><a href="{{ path('comentarios_show', { 'id': comentario.id }) }}">{{ comentario.id }}</a></td>
                <td>{{ comentario.comentario }}</td>
                <td>{% if comentario.fechaCreacion %}{{ comentario.fechaCreacion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if comentario.fechaActualizacion %}{{ comentario.fechaActualizacion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('comentarios_show', { 'id': comentario.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('comentarios_edit', { 'id': comentario.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('comentarios_new') }}">Create a new comentario</a>
        </li>
    </ul>
{% endblock %}
